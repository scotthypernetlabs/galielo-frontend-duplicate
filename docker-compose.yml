version: "3"
services:
  landing-zone:
    image: hypernetlabs/landing-zone-daemon
    container_name: landing-zone-daemon
    command: --username 'johndoe@hyperdyne.io' --password 'copied3077amazE' --backend 'https://profound-ripsaw-232522.appspot.com' --machine-id 'MID-1' --machine-name 'johndoe@hyperdyne.io Docker World Test Machine'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
